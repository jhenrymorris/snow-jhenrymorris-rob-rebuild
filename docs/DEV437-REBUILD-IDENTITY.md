# dev437065 Rebuild Identity

## Purpose

This branch is a controlled rebuild of the approved HR Access ROB
Authorization Fluent source under a new, platform-created application identity.
It exists to determine whether a clean `sys_app`/`sys_scope` parent resolves the
Australia Fluent installer failure without altering the original application.

## Authoritative identity

| Property | Value |
| --- | --- |
| Instance | `dev437065` |
| SDK auth alias | `pdi` |
| Application | `HR Access ROB Authorization V2` |
| Scope | `x_2166123_rob_auth` |
| Application / scope sys_id | `4aba8657837a43104f5193a6feaad3c5` |
| Package | `x-2166123-hr-access-rob-authorization-v-2` |
| Source branch | `codex/dev437-rebuild-identity` |

The scope and scope ID were generated by ServiceNow IDE's supported
`Create an app` workflow from the instance publisher prefix. They must not be
edited manually. The original application scope
`x_2108496_hr_acces`, its sys_id, its Git branch, and `dev285962` remain
unchanged.

## Bootstrap evidence

- A preliminary Studio shell (`6d508ed7833643104f5193a6feaad372`, scope
  `x_2166123_hr_acc_0`) proved unsuitable for Fluent source: it was marked
  `ide_created=SNS`, had no `package_json` binding, and did not appear in the
  IDE's Open Apps list. It is retained temporarily as failed-bootstrap
  evidence and is not the authoritative rebuild application.
- ServiceNow IDE created the authoritative minimal `now-sdk boilerplate`
  project in the dedicated `ROB DEV437 REBUILD` workspace. Its generated
  `now.config.json` contains scope `x_2166123_rob_auth` and scope ID
  `4aba8657837a43104f5193a6feaad3c5`.
- Before installation, read-only `sys_scope` verification correctly returns no
  record for the IDE scaffold. The application parent must be created by the
  supported Fluent Build and Install path; no shell record will be fabricated.
- No application source has been installed from the local SDK.

## IDE source synchronization result

- Final identity commit `3f3595d` is pushed to the isolated repository
  `jhenrymorris/snow-jhenrymorris-rob-rebuild` on `main`.
- The IDE Git extension has no supported `Add Remote` or repository-file import
  command. Its supported `Git: Clone` binding operation returned HTTP 500.
- Platform logs show `sn_glider.GliderGitRelay` requested the repository at
  `2026-08-24 18:39:58` and then raised
  `ECCResponseTimeoutException` after 30 seconds for ECC request
  `5dbc4e1383fa43104f5193a6feaad3b7`.
- `Git: Apply` is a stash operation, not a patch-file importer, so it cannot
  load the committed source without the Git relay.
- The incomplete local IDE checkout was removed from `ROB DEV437 REBUILD`
  through `Workspaces: Remove Application from Workspace...`. The workspace is
  empty again, and read-only verification still returns no `sys_scope` row for
  `4aba8657837a43104f5193a6feaad3c5` / `x_2166123_rob_auth`.

## Source reconciliation

- Executable Fluent source, server adapters, manual HR Core bridge source, and
  local validation fixtures use the new scope.
- `src/fluent/generated/keys.ts` was deleted only on the rebuild branch and
  regenerated by the ServiceNow SDK. It was not edited manually.
- Historical M2/M3 evidence retains the original scope because those events
  occurred against the original application.

## Local gate at bootstrap

- Normal SDK build: PASS with the five previously accepted TS11 simple
  reference-qualifier warnings.
- Frozen-key SDK build: PASS with the same five warnings.
- M2: 19/19.
- R1: 9/9 using the new SDK-generated seed identities.
- Wave 2 security: 22/22.
- Deployment configuration: 16/16.
- R3: 30/30.
- R4: 58/58.
- M4: 26/26.
- `git diff --check`: PASS.

## Installation boundary

This record is source/build evidence only. It is not proof of installation or
runtime behavior. Build and Install was not run. The rebuild is blocked before
source synchronization by the instance Git relay timeout; no manual source
injection, SDK installation, or fabricated `sys_app`/`sys_scope` record was
used.

Current classification:

- `M3 — BLOCKED-PLATFORM`
- `ServiceNow IDE Git relay source synchronization — FAILED`
- `M4 — NOT READY`
